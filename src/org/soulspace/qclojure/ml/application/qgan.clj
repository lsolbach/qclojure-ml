(ns org.soulspace.qclojure.ml.application.qgan
  "Common infrastructure for quantum generative adversarial networks (QGANs)"
  (:require [org.soulspace.qclojure.application.backend :as backend]
            ))